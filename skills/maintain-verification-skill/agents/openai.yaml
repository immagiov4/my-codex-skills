interface:
  display_name: "Maintain verification skill"
  short_description: "Audit and refresh a runtime verification skill"
  default_prompt: "Use $maintain-verification-skill to audit and refresh this project's verification workflow."
policy:
  allow_implicit_invocation: false
