interface:
  display_name: "TDD"
  short_description: "Prove a bug fix with a focused regression test"
  default_prompt: "Use $tdd to reproduce this bug with a focused failing test before fixing it."
policy:
  allow_implicit_invocation: false
