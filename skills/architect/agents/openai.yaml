interface:
  display_name: "Architect"
  short_description: "Design module boundaries before implementation"
  default_prompt: "Use $architect to design the types and module boundaries before implementation."
policy:
  allow_implicit_invocation: false
