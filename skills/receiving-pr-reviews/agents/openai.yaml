interface:
  display_name: "Valutare le osservazioni di revisione"
  short_description: "Triage review findings without scope spirals"
  default_prompt: "Use $receiving-pr-reviews to evaluate findings from a PR, local agent, or self-review before applying corrections."

policy:
  allow_implicit_invocation: false
